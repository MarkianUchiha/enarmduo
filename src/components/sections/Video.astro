---

---

<section class="w-full min-h-screen flex items-center justify-center px-4 md:px-8 py-4 md:py-8" style="background-color: var(--color-blanco);">
	<div class="w-full md:w-[90%]">
		<lite-youtube 
			videoid="VaABIKi7Cg8"
			videotitle="EnarmDuo - Plataforma Educativa"
			class="video-container"
			posterloading="lazy"
		>
			<a class="lite-youtube-fallback" href="https://www.youtube.com/watch?v=VaABIKi7Cg8">
				Ver en YouTube: "EnarmDuo - Plataforma Educativa"
			</a>
		</lite-youtube>
	</div>
</section>

<script>
	import '@justinribeiro/lite-youtube';
</script>

<style>
	.video-container {
		border-radius: 1rem;
		overflow: hidden;
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
	}

	.video-container:hover {
		transform: translateY(-4px);
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
	}

	/* Estilos de fallback para cuando JS está deshabilitado */
	.lite-youtube-fallback {
		aspect-ratio: 16 / 9;
		display: flex;
		justify-content: center;
		align-items: center;
		flex-direction: column;
		gap: 1em;
		padding: 2em;
		background-color: var(--color-negro);
		color: var(--color-blanco);
		text-decoration: none;
		border-radius: 1rem;
		font-family: 'Inter', sans-serif;
		font-weight: 600;
		transition: all 0.3s ease;
	}

	/* Icono de play triangular */
	.lite-youtube-fallback::before {
		display: block;
		content: '';
		border: solid transparent;
		border-width: 2em 0 2em 3em;
		border-left-color: var(--color-azul);
	}

	.lite-youtube-fallback:hover {
		background-color: #1a1a1a;
	}

	.lite-youtube-fallback:hover::before {
		border-left-color: var(--color-blanco);
	}

	.lite-youtube-fallback:focus {
		outline: 2px solid var(--color-azul);
		outline-offset: 4px;
	}

	/* Responsive: ajustar padding en móviles */
	@media (max-width: 640px) {
		.video-container {
			border-radius: 0.75rem;
		}
	}
</style>
